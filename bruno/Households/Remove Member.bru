meta {
  name: Remove Member
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/households/members/:memberId
  body: none
  auth: bearer
}

params:path {
  memberId: REPLACE_WITH_MEMBER_USER_ID
}

auth:bearer {
  token: {{accessToken}}
}
