spring.application.name=moni
jwt.secret=${JWT_SECRET:dev-only-insecure-secret}
aws.url=http://localhost:4567
aws.region=eu-central-1
# Force IPv4 for Android emulator compatibility
server.address=0.0.0.0